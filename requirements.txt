# Python dependencies for the EMIT water-quality product workflow.
# Pinned to the versions verified to work; relax as needed.
#
# NOTE: ACOLITE (atmospheric correction, L1B radiance -> L2W Rrs) ships its own
# bundled Python runtime and is not pip-installable, but HyperCoast downloads
# and runs it for you (hypercoast.download_acolite / run_acolite). No manual
# install is needed; set ACOLITE_DIR to reuse an existing install.

# Deep learning / inference
torch>=2.6.0
pytorch-lightning>=2.5.1
scikit-learn>=1.8.0

# Numerics
numpy>=2.0
scipy>=1.15

# Geospatial raster / COG
rasterio>=1.4
rio-cogeo>=5.4
rioxarray>=0.19

# NetCDF / arrays
xarray>=2025.3
netCDF4>=1.7
h5netcdf>=1.6

# EMIT search & download, gridding helpers
hypercoast>=0.24
earthaccess>=0.14

# Publishing products to the Hugging Face Hub
huggingface_hub>=0.26

# Tables & plotting (used by code/plot_and_save.py)
pandas>=2.2
matplotlib>=3.10
seaborn>=0.13
